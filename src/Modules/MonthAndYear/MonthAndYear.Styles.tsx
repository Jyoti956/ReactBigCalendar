import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  monthdiv: {
    textAlign: 'center',
    color: 'green',
    marginTop: '10px',
    marginLeft: '24%'
  }
}));
