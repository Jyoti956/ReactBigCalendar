import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    
    h3: {
        float: 'right',
        marginRight: '5%',
        color: 'blue'
      },
    
  }));